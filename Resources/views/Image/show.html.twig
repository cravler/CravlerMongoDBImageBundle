<img src="{{ path('image_show', {'imageData': imageDataEncode(groupId, width, height, mode|default('inset'))}) }}{{ noCache|default(false) ? ':nocache' : '' }}"{{ id|default('') ? [' id="', id, '"']|join|raw : '' }}{{ class|default('') ? [' class="', class, '"']|join|raw : '' }}{{ style|default('') ? [' style="', style, '"']|join|raw : '' }} alt="{{ alt|default('') }}" />
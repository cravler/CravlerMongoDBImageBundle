<img src="{{ path('image_show', {'imageData': imageDataEncode(groupId, width, height, mode|default('inset'))}) }}{{ noCache|default(false) ? ':nocache' : '' }}"{{ id|default('') ? [' id="', id, '"']|join : '' }}{{ class|default('') ? [' class="', class, '"']|join : '' }}{{ style|default('') ? [' style="', style, '"']|join : '' }} alt="{{ alt|default('') }}" />
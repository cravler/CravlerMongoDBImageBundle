<img src="{{ path('image_show', {'imageData': imageDataEncode(groupId, width, height, mode is defined ? mode : 'inset')}) }}{{ noCache is defined ? ':nocache' : '' }}" id="{{ id is defined ? id : '' }}" class="{{ class is defined ? class : '' }}" />